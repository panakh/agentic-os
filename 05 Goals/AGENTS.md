# Goals

Your goals: the altitude above projects (GTD / 12-week-year). One file per goal. Projects and next actions serve a goal. Keep this small; a handful of live goals, not a wishlist.

Each goal file carries frontmatter the dashboard reads:
`target` (the number), `done` (real progress only, never invented), `deadline`, `status`.
