# Inbox

The landing zone. Drop anything here: notes, links, half-thoughts, files. The inbox-processing routine reads each item, files it where it belongs, and archives the original. Notes typed into the studio land here too.
