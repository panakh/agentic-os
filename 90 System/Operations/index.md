# Operational Wiki (index)

Your SOPs and custom operating knowledge: how YOU run things. Complements the reusable skills in `.claude/skills/` (skill = general procedure; a page here = your custom SOP). See the `llm-wiki` skill.

Layout: one concept per page, named for the SOP/concept, grouped in topic folders (`<topic>/<sop-name>.md`), interlinked. This index lists every page.

## SOPs
- (none yet. Add your first: "add an SOP for X" - it becomes a small concept page here.)
